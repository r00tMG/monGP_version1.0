
services:
  mongpversion1:
    image: mongpversion1
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 4200:4200
    volumes:
      - ./:/angular-app
    environment:
      - STYLE=css

